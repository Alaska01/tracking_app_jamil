<p style="color: green"><%= notice %></p>

<h1>Movements</h1>

<div id="movements">
  <% @movements.each do |movement| %>
    <%= render movement %>
  
  <% end %>
</div>

<%= link_to "New movement", new_customer_movement_path, class: "btn bg-info" %>
<%= link_to "Back To Customer", customer_path(@customer), class: "btn bg-success" %>
